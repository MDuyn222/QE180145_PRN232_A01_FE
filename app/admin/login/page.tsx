"use client";import{FormEvent}from"react";import{useRouter}from"next/navigation";import{api}from"@/lib/api";import{toast}from"sonner";export default function Login(){const r=useRouter();const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const x=await api.post("/api/auth/login",{email:f.get("email"),password:f.get("password")});localStorage.setItem("token",x.data.token);toast.success("Login successful");r.push("/admin")}catch{toast.error("Invalid credentials")}};return <div className="mx-auto max-w-md card"><h1 className="mb-6 text-3xl font-bold">Admin login</h1><form onSubmit={submit} className="space-y-4"><input className="input" name="email" type="email" defaultValue="admin@simpleshop.com" required/><input className="input" name="password" type="password" defaultValue="@@Admin123@@" required/><button className="btn-primary w-full">Login</button></form></div>}
