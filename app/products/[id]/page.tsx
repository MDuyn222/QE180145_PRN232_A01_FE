"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{api,Product,money}from"@/lib/api";export default function Page(){const{id}=useParams();const[p,setP]=useState<Product|null>(null);useEffect(()=>{api.get(`/api/products/${id}`).then(x=>setP(x.data))},[id]);if(!p)return <p>Loading...</p>;return <div className="grid gap-8 md:grid-cols-2"><img src={p.imageUrl||"https://placehold.co/800x600?text=Product"} className="w-full rounded-3xl"/><div><p className="text-blue-600">{p.categoryName}</p><h1 className="mt-2 text-4xl font-bold">{p.productName}</h1><p className="mt-5 text-slate-600">{p.description}</p><p className="mt-6 text-3xl font-bold text-blue-600">{money(p.price)}</p><p className="mt-2">Stock quantity: {p.stockQuantity}</p></div></div>}
