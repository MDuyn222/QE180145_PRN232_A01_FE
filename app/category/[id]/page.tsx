"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{api,Product}from"@/lib/api";import ProductCard from"@/components/ProductCard";export default function Page(){const{id}=useParams();const[p,setP]=useState<Product[]>([]);useEffect(()=>{api.get(`/api/products/category/${id}`).then(x=>setP(x.data))},[id]);return <><h1 className="mb-6 text-3xl font-bold">Products</h1><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{p.map(x=><ProductCard key={x.productId} p={x}/>)}</div>{!p.length&&<p>No active products found.</p>}</>}
